import { Room } from "./types";
import Center from "../../components/Center";

interface DeliberatingProps {
  room: Room;
}

const Deliberating = ({ room }: DeliberatingProps) => {
  return <Center></Center>;
};

export default Deliberating;
